"""
Cross-Seed Cleaner — local overrides template.

Copy this file to config.local.py (same directory) and put ONLY the settings
you want to change from the config.py defaults in it. config.local.py is
gitignored, so a git pull never conflicts with your edits, while new settings
added upstream still arrive in config.py with their defaults and comments.

Precedence: config.py < config.local.py < environment variables < CLI flags.
Set LOCAL_CONFIG=/path/to/file to load the overrides from elsewhere, or
LOCAL_CONFIG="" to skip them entirely.
"""

# QBITTORRENT_HOST = "http://192.168.1.10:8080"
# QBITTORRENT_API_KEY = "qbt_..."
# HTML_EXPORT = "/srv/reports/output_{mode}_{run}_{datetime}.html"
